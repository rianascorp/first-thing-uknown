name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

env:
  APP_NAME: SimpleRun
  MAIN_CLASS: com.rianascorp.main.Main
  JAVA_VERSION: '17'

jobs:
  build:
    name: Build (${{ matrix.platform }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            platform: windows
            type: exe
          - os: macos-latest
            platform: mac
            type: dmg
          - os: ubuntu-latest
            platform: linux
            type: deb

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: temurin
          cache: maven

      - name: Test GitHub Packages access
        shell: bash
        run: |
          echo "Testing access to RianaLibraries..."
          curl -I \
            -H "Authorization: token ${{ secrets.PACKAGES_TOKEN }}" \
            https://maven.pkg.github.com/rianascorp/RianaLibraries/org/rianalibraries/rianalibraries/1.0.1/rianalibraries-1.0.1.pom
      - name: Set up Maven settings for GitHub Packages
        shell: bash
        run: |
          mkdir -p ~/.m2
          cat > ~/.m2/settings.xml << EOF
          <settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
                 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                 xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0
                 http://maven.apache.org/xsd/settings-1.0.0.xsd">
           <servers>
               <server>
                   <id>github</id>
                   <username>rianascorp</username>
                   <password>${{ secrets.PACKAGES_TOKEN }}</password>
               </server>
           </servers>
           <repositories>
               <repository>
                   <id>github</id>
                   <url>https://maven.pkg.github.com/rianascorp/RianaLibraries</url>
               </repository>
           </repositories>
          </settings>
          EOF

      - name: Build with Maven
        run: mvn -B clean package -DskipTests
        env:
          GITHUB_TOKEN: ${{ secrets.PACKAGES_TOKEN }}
          MAVEN_OPTS: "-Dmaven.wagon.http.ssl.insecure=true -Dmaven.wagon.http.ssl.allowall=true"

      - name: Find the main JAR
        id: jar
        shell: bash
        run: |
          JAR=$(find target -name "*.jar" ! -name "*-sources.jar" ! -name "*-javadoc.jar" ! -name "original-*" | head -n 1)
          echo "jar=$JAR" >> $GITHUB_OUTPUT
          echo "Found JAR: $JAR"

      - name: Package with jpackage
        shell: bash
        run: |
          mkdir -p dist
          if [ "${{ matrix.platform }}" = "windows" ]; then
            jpackage \
              --type exe \
              --name "${{ env.APP_NAME }}" \
              --input target \
              --main-jar "$(basename ${{ steps.jar.outputs.jar }})" \
              --main-class "${{ env.MAIN_CLASS }}" \
              --dest dist \
              --win-dir-chooser \
              --win-shortcut \
              --win-menu \
              --win-per-user-install \
              --verbose
          elif [ "${{ matrix.platform }}" = "mac" ]; then
            jpackage \
              --type dmg \
              --name "${{ env.APP_NAME }}" \
              --input target \
              --main-jar "$(basename ${{ steps.jar.outputs.jar }})" \
              --main-class "${{ env.MAIN_CLASS }}" \
              --dest dist \
              --mac-package-name "${{ env.APP_NAME }}" \
              --verbose
          else
            jpackage \
              --type deb \
              --name "${{ env.APP_NAME }}" \
              --input target \
              --main-jar "$(basename ${{ steps.jar.outputs.jar }})" \
              --main-class "${{ env.MAIN_CLASS }}" \
              --dest dist \
              --linux-shortcut \
              --verbose
          fi

      - name: Upload installer
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.APP_NAME }}-${{ matrix.platform }}
          path: dist/*
          if-no-files-found: error

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*
          generate_release_notes: true
          draft: false
          prerelease: false